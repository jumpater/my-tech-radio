# tech-radio 2026-09-16

## オープニング

tech-radio、2026年9月16日分です。今日は、脆弱性修正AIの成功率が26%にも86%にもなるのは測定条件が違うだけだと5つの罠で整理した記事、auto_incrementの主キーを環境間の突き合わせに使い本番35ページを事故で上書きしてしまった顛末、3人で6リポジトリを回すためにCLAUDE.mdを「憲法」に格下げし.planningと.serenaへ役割分担した指示書設計、Kiro Web Automationsに人工的な18件の不具合を仕込んで7日間保守を任せたら検出率100%・修正成功率87.5%だった検証、AWSの公開サンプルで環境変数を揃えたつもりでもBedrockが呼ばれずローカル生成にフォールバックしていた見分け方、AIペネトレーションテストツールをAWS以外のクラウドやLLM基盤で動かすためのガードレール設計、RAGの検索精度が母集団増加で崩れた「こじつけ検索」を類似度閾値とクエリ英語化で直した記録、Slackへの一言からInstagram投稿案10件をNotion連携で下書きする個人開発、DELETEのWHERE忘れとTRUNCATE誤実行を実行前に止める手順と復旧の現実、Goのフラットパッケージにファイル単位privateを機械的に強制するLinter declscope、encoding/json/v2をコード生成で最速級に仕立てるodjson、既存HTTP APIにGenkit for GoのFlowで型と観測の境界を引き直す設計、MWAAの通知設計をLambda Durable ExecutionのS3直接読み取りで6MB制限から解放した続編、さくらのウェブアクセラレータの料金体系を逆手に取ったほぼ無料のストリーミング収集基盤、FIDO2の非対称暗号による認証の仕組みをGoコードで追った解説、そしてMeshtasticの公式PythonライブラリをよそにGoでTUIチャットクライアントを自作した個人プロジェクトまで、16本お届けします。

## 1. AI修正パッチの「成功率26%」をどう読むか：Agent評価の5つの罠

kiritani_rさんの記事。AIエージェントに脆弱性修正をさせた際の成功率について、1Passwordの26%とTrail of Bitsの86%という一見矛盾する数値を比較検証した。1Passwordは6件の脆弱性に対して2種類のモデルで6,080件のパッチを評価し「既存欠陥の修正+新規欠陥ゼロ」を成功と定義したのに対し、Trail of Bitsは3,067件中2,634件が与えられたexploitを防いだかどうかで再集計しており、そもそも測っている対象が違うのだと指摘する。さらに自動採点と人間レビューの一致率は65.9%にとどまり、元の脆弱性修正の判定では87.7%一致する一方、新規バグ導入の判定では70.5%まで下がるという採点自体の不確実性も明らかにした。著者は実行条件の混在、成功定義の曖昧さ、失敗と判定不能の混同、評価者の信頼性未検証、一発正解率だけを見てプロセス全体を無視する、という5つの罠を挙げ、数字だけを比較するのではなく「何を測定対象にしたか」「何を成功と定義したか」「誰が判定したか」を確認しなければ、AIにどこまで任せてよいかの境界は見えてこないと結論づけている。

出典: https://zenn.dev/kiritani_r/articles/90523358108f8f

## 2. 環境をまたぐデータ移行で auto_increment の主キーを突き合わせに使ってはいけない

autocampさんの記事。自作CMSで運用しているサイトのデザインを全面刷新し、ローカルで作った内容を本番へ反映した直後、本番の35ページが別ページの中身に上書きされるという事故を起こした記録。原因は、テーブルのauto_increment主キーをローカルと本番の突き合わせキーに使ったことで、半年間別々に運用してきた2つの環境では同じIDが別のコンテンツを指していたためだった。著者はAIに作業を指示したところAIは指示通り35件を寸分違わず正確に処理してしまい、人間なら抱くはずの「この突き合わせで本当に正しいのか」という疑問が一切挟まらなかった点を、正確な実行能力が誤った指示の危険性をむしろ増幅させた例として振り返る。再発防止策として、環境をまたぐ突き合わせにはauto_incrementではなくURLパスなどの自然キーを使うこと、本番反映後はHTTPステータスだけでなく本文内容まで機械的に検証すること、AI作業中はローカル環境に触れないことを挙げている。

出典: https://zenn.dev/autocamp/articles/7f0127b4e417ff

## 3. Claude Codeに「指示書」を設計する — 6リポジトリを3人で回すCLAUDE.md/.planning/.serena

junghunさんの記事。3人のチームで6つのリポジトリを管理する中、セッションのたびに同じ説明を繰り返す作業に月20時間を費やしていたことに気づき、それ自体が自動化すべき手作業だと捉え直してClaude Codeへの指示書を3層に設計し直した記録。CLAUDE.md(憲法)は数ヶ月単位でしか更新せず毎セッション読み込まれるため200行以内に抑え、禁止事項と絶対ルールだけを具体的に書く。.planning/(実行計画)は週単位で更新し必要な時だけ読ませ、完了条件と触ってよい範囲を必須記載にして、完了条件がそのままテスト名になるくらいの粒度で書く。.serena/memories/(決定の記録)はセッション単位で、コードから読み取れない「なぜその選択肢を捨てたか」という失敗の記録だけを残す。著者は当初CLAUDE.mdを強制的なガードレールだと考えていたが、実際にはユーザーメッセージとして扱われ厳密な遵守は保証されないと分かり、本当に禁じたい操作は権限設定やフック側に実装すべきだと認識を改めたという。足すより消す方が効く、禁止事項の方が指示より効く、最初から完璧を目指さず事故のたびに1行ずつ足していく、という運用を通じて、セッション開始時の説明時間がほぼ消えたとまとめている。

出典: https://zenn.dev/junghun/articles/claude-code-instruction-design

## 4. 寝ている間にAWSリポジトリは保守できる？ Kiro Web Automationsを7日間・18 Issueで検証した

nobu_13さんの記事。AWSサーバーレスリポジトリの保守作業をAI自動化ツール「Kiro Web Automations」に任せ、人が毎朝レビューするだけの運用に近づけられるかを、人工的に仕込んだ18件の問題を使って7日間検証した記録。検出率は18件全てを見つけて100%、自動修正の成功率は16件中14件の87.5%、誤検知は0件で、人間が費やした作業時間は7日間で約35分だったという。一方で、テストとスキャナがすべて合格していても意味的には過剰な修正が起こり得ることも分かり、5日目にはセキュリティスキャナが何も検出しなかったにもかかわらず、AIが本来維持すべき機能を削除してしまう事例があった。著者は完全自動化ではなく、AIには低リスクな変更と検証を任せ人間が朝にレビューする運用モデルを推奨し、IAM権限やネットワーク公開のような高リスクな変更には自動修正を事前に制限しておく必要があると述べている。検証対象は小規模なサーバーレスアプリケーションであり、複雑な大規模インフラでは結果が変わる可能性がある点も断っている。

出典: https://zenn.dev/nobu_13/articles/6cd10f09e3eeaf

## 5. AWSのsample-contextforgeを実測 — Bedrock実呼び出しはbackend表示で見分けられる

nttdata_techさんの記事。AWSの公開サンプルaws-samples/sample-contextforgeには、AWS資格情報がなくても動くようローカルのSQLiteとオントロジーだけで完結するモードと、Amazon Bedrockを実際に呼び出すモードが切り替えられる仕組みがあり、環境変数を設定したつもりでも実は呼ばれていなかったという取り違えが起こり得ることを実測で確かめた記事。生成側はGENERATION_BACKENDとAWS_REGIONの両方の環境変数が揃って初めてBedrock呼び出しが発生し、どちらか一方だけではローカルのテンプレート生成にフォールバックしてしまう。2026年9月9日にus-east-1とus-west-2で6条件を実行しCloudTrailのInvokeModelイベントと突き合わせたところ、最も信頼できる判別点はGraphRAGパネル右上のバッジで、(bedrock)か(simulated)かがそこだけに明示される一方、同じ画面のパイプライン図は検索結果が1件でもあれば常に「model via Bedrock」と表示されてしまい判別に使えないことが分かった。さらに埋め込み側はAWS_REGIONが未設定でも警告なくコード既定のus-west-2へ自動的にBedrock呼び出しを試みるため、気づかないまま想定と異なるリージョンで課金が発生し得るとも指摘し、UI表示・サーバ出力・CloudTrailという複数の観測層を組み合わせて初めて実呼び出しの有無を正確に把握できると結論づけている。

出典: https://zenn.dev/nttdata_tech/articles/78fbb937bd1d89

## 6. AI Agentを用いたペネトレーションテストツールのクラウド適用例

nttdata_techさんの記事。AWS Security Agentが使えない他クラウドや、Bedrock以外のLLM基盤を使いたいケースを想定し、OSSのAIペンテストツール「Shannon」をAWS環境に実装した事例。クラウド環境でAIペンテストを行う際は、各クラウドが定める許可範囲を守りDNS乗っ取りやDoS攻撃のような禁止行為を避けること、脆弱性やソースコード情報が外部に漏れないよう通信先をホワイトリスト化すること、Claudeなどのモデルにはサイバー攻撃防止の自動ブロック機能があるため正当な防御目的でもCyber Verification Programへの事前申請が要ることという3つの課題を整理する。実装ではDocker Sandboxes(隔離microVM)でツールを実行し、HAProxyで転送先をALBとLiteLLM APIだけに絞り、HTTPとTCP接続にレート制限をかけて過負荷を防止し、AWS側もECSタスクにタスクロールを与えずS3権限を最小化するなど、internet-facing ALBへのテストが事前承認不要という許可範囲の中でガードレールを重ねた。実際にShannonを動かしてXSSエクスプロイトの成功を確認できた一方、最も安価なHaikuモデルでも完全スキャンには20ドル以上のコストがかかることも分かったという。

出典: https://zenn.dev/nttdata_tech/articles/4f3e6dc2885ddd

## 7. Strands Agents + BedrockのRAG PoCで起きた「こじつけ検索」を、類似度閾値とクエリ英語化で解消した

satoru_oさんの記事。Strands AgentsとAmazon Bedrockを組み合わせたRAGのPoCで、検索対象の文書を3件から60件近くまで増やしていく過程で発生した検索精度の問題を解消した記録。当初の実装は類似度の閾値を設けず常に上位2件を固定で返す作りだったため、母集団が増えると2件目に無関係な記事が選ばれてしまう「こじつけ検索」が起きた。類似度0.5以上でフィルタする方式に変えてみたものの、日本語のクエリと英語の文書を同じ埋め込みモデルで扱っていたためスコアが0.58前後に密集し、クエリの言い回しがわずかに変わるだけでヒット件数が不安定になった。最終的にはエージェントへの指示でrag_searchツールを呼ぶ前にクエリを英語へ翻訳させ、クエリと文書の言語を揃えることで閾値0.5でも安定してヒットするようになり、該当がない場合は無理に検索結果を返さない堅牢な実装にたどり着いたという。

出典: https://zenn.dev/satoru_o/articles/2909b96f820a4e

## 8. Slackに一言で企業Instagramの投稿案が10件出る仕組みを、AWSサーバーレス×Claudeで作った

takutoshutoさんの記事。専門の開発部署を持たない会社でEC・Webマーケティングと社内システム開発をひとりで兼任する著者が、片手間で回していた企業Instagramの運用を、Slackにひとことメッセージを送るとAIがNotionの情報を参照して月10件の投稿案を下書きするAWS Lambda(Python)ベースの仕組みに置き換えた記録。設計の軸に据えたのはプロンプトの磨き込みよりもAIが参照するNotionデータベースの事実の整備で、架空情報の混入を防ぐため会社情報やテーマを「確度」フィールド付きのカードとして構造化している。AgentRequestを受けてAgentResultを返すという共通インターフェースで複数エージェントを疎結合に設計し、投稿や課金を伴う操作は必ず人が最終確認する体制とし、冪等キーやサーキットブレーカー、AWS Budgetsの二重アラートで夜中の暴走を防ぐ多層防御も組み込んだ。Slackの3秒タイムアウトに対応するため受付用Lambdaで即座にACKを返し重い処理は別Lambdaで非同期実行する二段構成にしたほか、UTC/JSTの日付ズレで冪等キーが衝突する、SAMデプロイが手動変更を上書きする、生成する画像のキャラクターが毎回別人になるといった細かい落とし穴にも触れている。一方でMeta APIの取得のような月1回だけの作業は、トークン失効や仕様変更に追従する保守コストが見合わないとしてあえて自動化せず手入力のまま残したという。

出典: https://zenn.dev/takutoshuto/articles/86c04c2a747f17

## 9. DELETEのWHEREなし・TRUNCATE誤実行で全件消す事故を、実行前に止める手順と消した後の復旧

selene_nyx_aiさんの記事。DELETEのWHERE句忘れやTRUNCATEの誤実行というSQL運用の古典的な事故を、実行前に止める手順と消してしまった後の現実的な対処に分けて整理した記事。実行前の備えとしては、自動コミットに任せず明示的にトランザクションを開始してDELETE直後に一旦手を止め影響行数を確認すること、削除対象の件数とテーブル全体の行数を検算SELECTで突き合わせ両者が一致していたらWHERE句が効いていない危険な状態だと疑うこと、SQLを日本語に読み替えて意図と照合することの3手順を挙げる。DBMSごとの挙動差にも触れ、MySQLとOracleはTRUNCATEが暗黙コミットされロールバックできないのに対し、PostgreSQLとSQL Serverはトランザクション内であればTRUNCATEも取り消せるという違いを整理した。コミット済みの削除についてはアプリケーション側でできることはほぼなく、バックアップやPITRでの復旧、削除適用前に止められた遅延レプリカからの抽出、バイナリログなど環境依存のログが頼りになるとし、戻せるかどうかは事故が起きた瞬間ではなくその前のバックアップ設計で決まっていると結論づけている。

出典: https://zenn.dev/selene_nyx_ai/articles/sql-delete-all-rows-prevent

## 10. Go のフラットなパッケージにファイル単位の private を持ち込む Linter "declscope"

yumemi_incさんの記事。Goでは「パッケージは少なく大きく」というフラットパッケージの作法が重視される一方、可視性がexported/unexportedの2段階しかなくファイル単位のprivateが存在しないため、特定ファイル内だけで使うつもりのヘルパー関数がパッケージ全体から無制限に呼べてしまうという課題を、Linter「declscope」で機械的に検出する取り組みを紹介する記事。ファイル名から自動的に命名されるnamespaceという単位でファイルを管理し、privateな宣言が自分のnamespaceの外から呼ばれていないかを常時検査するboundaryルールと、namespaceに属する関数名がその所属を表しているかを問うqualifyルールを備える。違反を見つけた開発者は呼び出し側をnamespace内へ移動するか、コメントで明示的に共有物だと宣言するかの2択を迫られる仕組みで、既存コードベースに導入する際はbaselineで現状の違反を記録し新規の違反だけを報告できる。著者は、これまで人間の頭の中にしかなかった「このヘルパーはこのファイルだけで使う」という暗黙の規約が、AIエージェントには容易に踏み越えられてしまう時代だからこそ、その規約をソースコードの一部として機械可読にする意義があると位置づけている。

出典: https://zenn.dev/yumemi_inc/articles/go-declscope-file-scoped-private

## 11. encoding/json/v2 を最速の JSON エンコーダ/デコーダにする

mazreanさんの記事。Go標準のencoding/jsonはany型で引数を受け取りリフレクションを駆使して変換する設計のため速度に課題があり、既存の高速化策はリフレクション結果をキャッシュして導入は容易だが最適化に限界がある方式と、コード生成で型に特化した専用処理を作る代わりに大規模な書き換えが必要になる方式の2系統に分かれていたと整理する。著者が開発した「odjson」は、go:generateの1行を足すだけでencoding/json/v2のMarshalJSONTo/UnmarshalJSONFromメソッドを自動生成し、コード生成型の速度と標準ライブラリ互換性の両立を狙う。内部バッファへの直接読み書きでオーバーヘッドを避け、SIMD within a Register(SWAR)で8バイトずつ一括処理し、エンコードでは頻出パターンを16バイト単位の定数として書き込み、デコードでは決定木でのフィールド名判定とビットセットによる重複検出でメモリ確保と比較回数を削減する。Go 1.27.1・Ryzen 9 7950X環境でのベンチマークでは、エンコードで4倍以上、デコードで2.69〜3.39倍の高速化を確認し、最速級とされるbytedance/sonicと比べても総合的に上回る一方、マルチバイト文字処理はSIMD命令を使うsonicの方が優位だとしている。

出典: https://zenn.dev/mazrean/articles/27314fad0e5c73

## 12. Genkit for Goを既存HTTP APIに組み込む：フローを境界にして型と観測をそろえる

nonejpさんの記事。既存のGo製HTTP APIに生成AI機能を足すとき、モデルAPIをハンドラーから直接呼ぶ構成でも試作はできるが、入力形式・ストリーミング・トレース・公開方法を後からそろえようとすると責務の境界が曖昧になりがちだとし、Genkit for GoのFlowという単位で境界を引き直す方法を最小構成で確認した記事。Flowは「AIを使うかどうか」ではなく「どのデータ形式を受け取りどの形式で返すか」を明示する仕組みで、通常のGo関数をラップして型安全な入出力・ストリーミング・Developer UIとの連携・HTTPエンドポイント公開までを統一的に扱う。HTTP層をクライアントに公開する唯一の境界、Flow層を入出力型と業務処理とモデル呼び出しの管理単位、Model/Tool層を生成や外部API呼び出しの実行単位として分け、genkit.DefineFlowとgenkit.Handlerでフロー本体と公開を分離することで、入出力構造が明確になりテストやトレースをフロー単位でそろえられ、モデル呼び出しをフロー内部に封じ込めたままAI機能を後から追加・差し替えできるようになると述べている。

出典: https://zenn.dev/nonejp/articles/d47d7f9d66f930

## 13. MWAA通知設計をLambda Durable ExecutionとS3直接読み取りで拡張する

genegさんの記事。以前書いたMWAA(Managed Workflows for Apache Airflow)のタスク完了・失敗をEventBridge経由でLambdaに拾わせ「耐久性のあるステートマシン」として扱う通知設計の続編で、実行状態を保存したチェックポイントのペイロードが大きくなるとLambdaのイベントペイロード上限6MBに引っかかるという課題を、AWSが新しく発表した「Lambda now supports direct read configuration for Amazon S3 Files」で解消した記録。ペイロード本体をS3に置きLambdaのイベントにはS3のキーだけを渡す設計に変え、設定側でアクセスを宣言するだけでコード内で明示的にS3 SDKを呼ばずファイル操作できるようになった結果、boto3のget_object呼び出しが消えPath.read_textのような通常のファイル操作に置き換わり、状態遷移ロジックそのものに集中できるようになったという。EFSほど複雑でなくVPC構成やマウントターゲット管理も不要な軽量な解だとしつつ、IAMポリシーとdirect read設定で同じプレフィックスを指定してアクセス範囲を厳密に絞ること、S3ライフサイクルルールでチェックポイントの肥大化を防ぐこと、読み取りリクエスト数が多い場合はコスト試算が必要なことを運用上の注意点として挙げている。

出典: https://zenn.dev/geneg/articles/2026-09-14-aws-lambda-now-supports-direct-re

## 14. さくらのCDNの料金体系を逆手に取る 激安ストリーミング収集基盤の作り方

miyanagaさんの記事。さくらインターネットのウェブアクセラレータが「アウトバウンド転送量だけ課金され、インバウンドとリクエスト数は無料」という特殊な料金体系であることに着目し、Amazon Data Firehoseのようなストリーミングデータ収集基盤をほぼ無料で作る設計を紹介する記事。JavaScriptでユーザーデータをGETパラメータに詰め込みCDNへリクエストを送ると、CDNは1×1ピクセルの透過GIFのような数十バイトの最小限のレスポンスを返すだけでよく、送信データは全てインバウンド(無料)扱いのままアクセスログに記録されるため、後からログを解析して集計すればよい。AWSのData FirehoseやCloudFrontはリクエスト数やデータ量に応じて課金されるためこの手法は使えず、さくらの料金体系ならではの抜け道だとし、月間数億PV規模でも月額数千円程度に収まる一方、ログの反映には最大1時間のラグがあり、GETパラメータは約2KB程度に制限され、リアルタイム処理には向かないため日次・週次の集計用途に絞って使うべきだとまとめている。

出典: https://zenn.dev/miyanaga/articles/zenn-2026-09-16

## 15. How FIDO2 Works

giulianoさんの記事。パスキーやYubiKeyなどのセキュリティキー、Touch IDやWindows Helloに使われるパスワードレス認証の標準規格FIDO2の仕組みを、Goのコード例を交えて技術的に掘り下げた記事。FIDO2はブラウザ向けAPIであるWebAuthnと、認証器との通信プロトコルであるCTAP2から構成され、リライング・パーティ(サーバー)、クライアント(ブラウザ)、認証器の三者間でやり取りされる。核となる発想は共有秘密ではなく非対称暗号を使うことで、認証器側だけが秘密鍵を持ち、サーバーには公開鍵だけが登録される。登録フェーズと認証フェーズのそれぞれでサーバーが発行したチャレンジに認証器が秘密鍵で署名することで本人確認が行われ、go-webauthnライブラリを使った実装例では、ユーザー型の定義からリライング・パーティの設定、登録・認証の開始と完了処理までを段階的に示している。著者はセキュリティ上重要な点として、チャレンジの検証、署名カウンターの追跡によるクローン検知、RPIDを固定して別ドメインへのなりすましを防ぐことを挙げている。

出典: https://zenn.dev/giuliano/articles/12008f37101d63

## 16. Meshtasticの公式ライブラリはPythonだったのでGoでTUIチャットクライアントを自作した

kuroikotechさんの記事。LoRa無線を使いインターネットも携帯回線もなしにテキストチャットや位置情報をやり取りできるオープンソースのメッシュ通信プロジェクトMeshtasticで、公式ライブラリがPython製だったため、開発環境をGoに揃えターミナルに常駐させたいという個人的な動機からGo実装のクライアントライブラリを使いTUIチャットクライアントを自作した記録。ハードウェアはXIAO ESP32S3とWio-SX1262のキットを購入し、3Dプリントのケースに組み込んで使っている。デバイス未接続でもアプリが起動し続けttyACMやttyUSBといった標準ポートを優先的に探すシリアル接続の作りにしたほか、Charm社のbubbletea(Elm Architecture風の単一ゴルーチン設計)を使いチャット・ノードリスト・GPSダッシュボードの3ペイン表示を実装し、Haversine公式と方位角計算でノード間の距離と8方位を表示するGPS機能や、1000件を超えたら古いものから削除するJSON形式のメッセージ履歴保存も組み込んだという。

出典: https://zenn.dev/kuroikotech/articles/meshtastic-tui-go-bubbletea-client

## エンディング

今日はここまでです。kiritani_rさんのAgent評価の罠とnttdata_techさんのBedrock実呼び出し判別は、AIの数字や設定を鵜呑みにせず何を測っているかを確かめる大切さを、評価手法とインフラ観測という違う角度から見せていました。autocampさんのauto_increment事故とselene_nyx_aiさんのDELETE/TRUNCATE事故対応は、AIや人手を問わず正確な実行力がそのまま安全とは限らないという教訓を、データ移行とSQL運用という別の現場から裏付けていました。junghunさんの指示書設計とnobu_13さんのKiro検証は、AIエージェントにどこまで任せどこで人が確認するかという境界線を、CLAUDE.mdの3層構造と実測データという形でそれぞれ具体化していました。nttdata_techさんのペネトレーションテスト、satoru_oさんのRAGこじつけ検索、takutoshutoさんのInstagram投稿自動化は、AIを実運用に組み込む際のガードレールや事実整備の設計を、セキュリティ・検索・SNS運用というそれぞれ異なる文脈で見せてくれました。yumemi_incさんのdeclscope、mazreanさんのodjson、nonejpさんのGenkit for Go、genegさんのMWAA拡張は、Goのパッケージ設計から通信の境界の引き方まで、地道な設計判断の積み重ねが強さになることを教えてくれる回でした。miyanagaさんのCDN料金逆手取り、giulianoさんのFIDO2解説、kuroikotechさんのMeshtasticクライアント自作は、既存の仕組みを深く理解した上で自分の手で組み立て直すことの面白さを伝えてくれました。また明日。
