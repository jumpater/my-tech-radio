# tech-radio 2026-09-15

## オープニング

tech-radio、2026年9月15日分です。今日は、サブエージェントが読んだ3,093件のツール結果のうち親に届くのは1件もないという委譲の検証不可能性を数値で暴いた記事、5つの業務ドメインでAIが生成した宣言型アプリの制約が実は素通りだったと発覚した「宣言なきものは検知できない」というハルシネーション対策の連載、サンドボックスは檻ではなく能力を引き出す境界だと位置づけたエージェント実行環境の設計論、S3のバージョニングだけでは「戻すべきでない状態」を区別できないと指摘した障害復旧設計の記事、複数のClaude Codeを同時に走らせると進捗報告と作業番号がGitに載る前に衝突するという運用知見、17名のエンジニアへのアンケートからAIと人間の役割分担の温度差を可視化した記事、Kiroにアジェンティックなプルリクエスト修正を任せた実験でGuardrailsが被害範囲をゼロに抑えたという検証、NVIDIA製のセキュリティスキャナーSkillSpectorでAgent Skillsのリスクを事前に洗い出した試用記、社内QAをGoogle ChatとClaude Agent SDKとAWS Lambdaで組んで表面化しないタイプの失敗に何度もぶつかった構築記、CognitoのパスワードをAWS WAFがSQLインジェクションと誤検知しログイン不能になった原因追跡、大会本番でCPU飽和は防げたのに「効いた証拠」を残せなかった4コアVPSの反省記、SQSとLambdaの部分バッチ失敗をJavaで実装する設計、htmx4への移行がイベント名の書き方次第で牙を剥くという破壊的変更の実地検証、Goのdeferがヒープ確保・スタック確保・展開型という3つの方式で実装されている仕組みを追った連載初回、GherkinとAIエージェントでブラウザ操作テストを自動化し12シナリオで誤検知ゼロを達成した記録、そしてElevenLabs・Gemini・Microsoftの日本語文字起こし3社を実測比較した検証まで、16本お届けします。

## 1. サブエージェントは3,093件のツール結果を読み、親には1件も渡さない — だから報告は裏取りするしかない

tsutomusaitoさんの記事。389個のログファイルから138件のサブエージェント委譲を分析し、委譲先が読んだツール呼び出しは合計3,093件・出力トークン202万に上る一方、親エージェントに届くテキストは中央値でわずか1,151字、ツール結果に至っては1件も渡らないという非対称を数値で示した。委譲が「親の文脈を汚さない」という利点はそのまま「根拠を確認できない」という欠点の裏返しであり、報告に使われた原文はおよそ55%しか親に届かず、残り45%は内部処理で消えているという。著者は委譲そのものをやめるのではなく、数値報告には必ず一次データを裏取りし、裏取りできない仕事は委譲しない、要約ではなく「どこをどう見たか」という証拠を返させる運用に落とし込むべきだと結論づけている。

出典: https://zenn.dev/tsutomusaito/articles/subagent-report-verification-ja

## 2. 【第6回】AIのハルシネーションとの戦い — 宣言されなかった制約は強制されない

highfarさんの記事。ハルシネーション対策連載の第6回で、5つの業務ドメインにAIで宣言型アプリを生成させ、業務制約が実行時にどこまで効いているかを検証した。図書貸出では5つの制約を書いたのに生成されたスキーマの宣言は空配列で警告も0件、会議室予約では7制約中3つが表現できず「運用で担保する」という選択肢に流れた結果、実際にPostgreSQLで20並行テストをかけると二重予約が普通に通ってしまった。経費精算では申請→社員→部署という外部キー2段の参照が既存の宣言形式で表せず、初期管理者すら生成されず誰もログインできないアプリまで生まれたという。著者は、警告が0件であることは「宣言が正しい」ことの証明にはならず、宣言の不在と宣言の正しさを同一視してしまう危険を指摘し、警告が歯止めとして機能するには「従えるか」「従ったら消えるか」「消える条件と塞ぐ条件が一致するか」という3条件が要ると結論づけている。

出典: https://zenn.dev/highfar/articles/undeclared-constraints-are-not-enforced

## 3. エージェントの実力を引き出す、サンドボックスの設計

1amageekさんの記事。エージェントの実力はモデル単体では決まらず、実行環境=サンドボックスの設計が引き出せる能力を大きく左右するという主張のもと、サンドボックスを「檻」ではなく「自由を与える境界」と捉え直す設計論。危険なコマンドを名前で止める方式は、同じ動作をする別の書き方を防げない一方で無害な作業まで制限してしまうとし、ある実験ではテスターが危険なコマンドを見抜けたのは13.6%に留まりセッションが長くなるほど検知率が下がったのに対し、自動判定は89%を止めたというデータを紹介する。認証情報や本番データを物理的に隔離し、gVisorやmicroVMでカーネルを共有しない境界さえ確保すれば、その内側ではファイル操作もパッケージインストールもサーバー起動も自由にさせてよいという発想の転換を提案し、実行環境をセッションごとに使い捨てるのではなくファイルシステムやメモリ状態をスナップショットとして持ち越す流れが進んでいるとまとめている。

出典: https://zenn.dev/1amageek/articles/agent-sandbox-environment

## 4. 戻せる状態を、全部戻してはいけない

miyakonaさんの記事。障害復旧の設計では「何を復旧するか」より先に「何を復旧しないか」を決めるべきだという主張。蓄積型の台帳を旧版に戻すと過去の処理実績が失われる例、外部サービスへのアクセス再開時刻を旧版に戻すと安全のための猶予期間が逆に縮む例、旧版に無かった差分要素を単純上書きで消してしまう例を挙げ、時間で安全側に育っていく値は戻さない、通常運用と同じマージ規則で差分を保持するといった判断基準を示す。復旧手順も対応者が環境を止める段階、読み取り専用ツールで検証する段階、版と操作を選んで保存する段階、再検証して処理を再開する段階に分けて設計しており、バージョニング機能は判断材料を提供するだけで、戻すべき状態と残すべき現行差分と戻してはいけない状態を切り分けられて初めて復旧手順になると結論づけている。

出典: https://zenn.dev/miyakona/articles/restore-only-irreplaceable-state

## 5. Claude Code を複数同時に動かすなら、CLAUDE.md にこの2つを貼っておく

tomatomerdeさんの記事。複数のClaude Codeを並行稼働させると、コードそのものではなく作業一覧のようなメタ情報で衝突が起きるとして、CLAUDE.mdに足すべき2つの決め事を提案する。実際に、あるエージェントが前日10時に終えた作業が28時間後になっても「未着手」と報告された事例(完了分がメインブランチに未取り込みだっただけ)や、2つのエージェントが同時に次の作業番号を採番し重複させた事例を紹介し、対策として進捗報告前にgit rev-listで本流未取り込みの件数を数えて区別すること、番号採番はメインブランチの一時的なworktreeで行いGitの早い者勝ち機構で重複pushを弾くことを挙げている。

出典: https://zenn.dev/tomatomerde/articles/semantic-conflicts-outside-code

## 6. 何をAIに任せ、何を人間に残すか？ - 私たちの開発現場のAIとの役割分担

muit_techblogさんの記事。AIに何を任せ人間に何を残すかについて、17名のエンジニアにアンケートを取った結果をまとめた記事。AI寄りか人間寄りかを10段階で聞いたところ平均は2.71で82.4%が1〜3を選ぶ一方、「バックログを実装項目に落とし込む」工程は人間寄りの回答が最多、「品質・CIチェックと自動取り込み」はAI寄りが最多、レビューやテスト生成は意見が真っ二つに割れたという。若手は要件理解と計画を自分で担いたいと答え、POは要件調整と全容把握は人間の役目だと答えており、著者はAIには成果物作成を任せ、上流の意思決定・品質判断・成長機会は人間に残すという役割分担を提案し、レビュー機能や設計ガード、ペアプロモードをハーネス側で制御する実装を検討している。

出典: https://zenn.dev/muit_techblog/articles/709bc76c2aed94

## 7. KiroをCI/CDに組み込むと、AWS Pull Requestの「検出→修正→再検証」をどこまで委任できるか

nobu_13さんの記事。AIエージェントKiroにCI/CDでのプルリクエスト修正を任せ、失敗検出から修正・再検証までどこまで委任できるかを、ガイドなしのPure Kiroとガイドライン付きのGuardrailed Kiroの2条件×6シナリオ×5回、計60回の実行で比較した実験。自律修復率はPureが83.3%、Guardrailedが90.0%で統計的な有意差はなかったが、Pure条件では1行修正で済むはずの変更が81行の上書きと2件のリグレッションを生んだ事故が発生したのに対し、Guardrailed条件ではリグレッション発生率が0%に抑えられたという。失敗8件中7件はAIの推論不足ではなくheadless環境でのファイルアクセス権限の制限が原因だったとし、著者は成功率そのものより失敗時の被害範囲をどう縮めるかにGuardrailsの価値があると結論づけている。

出典: https://zenn.dev/nobu_13/articles/036fe8fdc7b046

## 8. 今話題の SkillSpector で agent skills の安全性をチェックしてみた

gemcookさんの記事。NVIDIA製のセキュリティスキャナーSkillSpectorを使い、Agent Skillsを導入する前に機械的にリスクを洗い出せるか試した記事。あるリポジトリをまるごとスキャンするとバージョン固定のない依存関係などが40件見つかりリスクスコア100点で「導入禁止」と判定された一方、同じリポジトリから1つのスキルだけを切り出してスキャンするとスコア0点の「安全」に変わったという。有害な指示を混ぜ込んだテスト用スキルや、キリル文字の同形文字やHTMLコメントで命令を隠したテスト用スキルはいずれも高いスコアで危険と正しく検出できたが、著者はツール単体では不十分で人による確認とAIによるダブルチェックを組み合わせる必要があると結論づけている。

出典: https://zenn.dev/gemcook/articles/try-skillspector-agent-skills-security

## 9. Google Chat × Claude Agent SDK × AWS Lambdaで「事業部向けのAIエージェント」を作った話

wwwaveさんの記事。Google ChatとClaude Agent SDK、AWS Lambdaを組み合わせ、基幹システムのコードや業務ドキュメントを読んで質問に答える社内QAエージェントを構築した記録。セッション情報はDynamoDB、会話履歴はS3で外部化し、用語登録はDynamoDBを経由してGitLab CIの夜間バッチが自動でMRを起票する構成にしているが、実装ではJWTが想定と異なるOIDC形式で届いたり、AgentDefinitionのtoolsとallowedToolsが別軸で両方の指定が必要だったり、会話履歴の保存パスが予想と違って検出に失敗したりと、エラーとして表面化しにくい種類の失敗に何度もぶつかったという。著者は生ログを直接確認することが最も有効な調査手段だったと振り返り、得られた知見は社内資産として将来の改修設計書自動生成などに応用する予定だとしている。

出典: https://zenn.dev/wwwave/articles/50f7450f5d13bf

## 10. 犯人はAWS WAF：CognitoのパスワードがSQLiと誤検知された話

dgtechさんの記事。ステージング環境の管理画面で特定の1アカウントだけがログインできず、通常の認証エラーすら出ない障害の原因を、AWSコンソールへのアクセス権なしに突き止めた記録。ログイン処理は例外を投げず認証エラーを戻り値で返す設計のため、全画面エラーは認証より手前で起きていると推測し、Next.jsの本番エラーに付くはずの識別子(digest)が欠けていたことからクライアント側の通信で失敗していると絞り込み、Terraformの設定を確認したところCloudFrontに設定されたAWS WAFのSQLインジェクション検知ルールがブロックモードで有効だったことが分かった。Cognitoのパスワードポリシーが要求する記号てんこ盛りの自動生成パスワードをSQL構文の断片と誤認識してブロックしていたのが真犯人で、対策としてサーバー側の例外処理でエラー文言を変換し、該当ルールを一旦カウントモードに落とし、根本的にはブラウザ側のSRP認証やCognitoのHosted UIを使ってパスワード文字列自体をネットワークに流さない構成へ移行するとしている。

出典: https://zenn.dev/dgtech/articles/aws-waf-blocks-cognito-password

## 11. 4コア4GBのVPSが大会負荷で限界を迎えた話 ― キャッシュを直しても飽和は残った

ckoshienさんの記事。4vCPU・4GBの共有VPS上で大会本番のアクセス負荷に対応した記録。事前にキャッシュの有効期限が同じタイミングで一斉失効する問題にジッターを入れて分散し、試合終了ごとに全キーを破棄していた処理を影響範囲だけの削除に絞り、キー生成のロジックが読み書きで食い違って毎回キャッシュを外していたバグを直すなど複数の対策を打ち、本番ではサーバーダウンこそ防げた。しかしvmstatで確認するとCPU実行待ちが10〜12という飽和状態が残っており、docker statsではMySQLがCPUの53〜69%を占有していたにもかかわらず、キャッシュヒット率を計測していなかったために対策が実際に効いていたのかを証明する材料が残せなかったという。著者は「落ちなくなった」ことと「修正が効いた」ことは別物であり、性能対策では改善を裏付ける計測をあらかじめ用意しておくことが欠かせないと結論づけている。

出典: https://zenn.dev/ckoshien/articles/vps-4core-tournament-load

## 12. Lambda × SQSで1件の失敗に全件巻き込まれない：ReportBatchItemFailuresをJavaで実装する

sponge841841さんの記事。SQSをイベントソースにしたLambdaのバッチ処理で、10件中1件が失敗しただけで成功した9件まで再試行されてしまう問題への対処をJavaで実装した記事。メッセージごとにtry-catchで例外を捕まえ、失敗したmessageIdだけを集めて返す構造に加えて、イベントソースマッピング側でReportBatchItemFailuresを明示的に有効化しないと部分再試行が機能しない点を指摘する。可視性タイムアウトはLambdaのタイムアウト設定の最低6倍を確保し、何度も失敗するメッセージはDLQに隔離するなど、部分バッチレスポンス・冪等性・可視性タイムアウト・DLQの4点をセットで設計する必要があるとまとめている。

出典: https://zenn.dev/sponge841841/articles/2026-09-14-lambda-sqs-partial-batch-response

## 13. htmx4はhtmx2のアプリの何を壊すのか

okamyujiさんの記事。公式が「ユーザーから見ればhtmx2とほぼ同じ」と説明するhtmx4への移行が、実際にはイベントハンドラの書き方次第で牙を剥くと指摘する記事。htmx2の`htmx:configRequest`が`htmx:config:request`に名前を変え、詳細情報の構造も変わったため、送信時にクライアントメッセージIDを付与するハンドラが無反応になり、サーバーが400エラーを返しても画面に何も表示されなくなった実例を紹介する。しかも公式の移行チェックツールはこのケバブケースの旧イベント名を検出できないという。ほかにも204と304以外の全応答をスワップするよう変わったためエラー文言がそのまま画面に挿入される問題や、WebSocket用の属性名変更などを挙げ、影響の大きさは要素ごとに属性を書く流儀かどうかで決まるとし、公式がhtmx2を無期限サポートすると明言している以上、急いで上げる技術的理由はないとまとめている。

出典: https://zenn.dev/okamyuji/articles/htmx4-what-breaks-in-htmx2-apps

## 14. Go の defer について調べてみた — 登録の仕組みと3つの実装方式【その1】

wakame_atsushiさんの記事。Goのdefer文が実行のたびに動的に登録される仕組みを、5回連載の初回として調査した記事。ループや条件分岐の中でdeferを呼んだときの挙動から、呼び出し対象の関数値と引数がその都度新しく評価・保存されることを確認したうえで、コンパイラが実際に使い分けている3つの実装方式を整理する。実行時に数が増える場合はヒープに`_defer`構造体を確保してランタイムのリストにつなぐheap-allocated方式、個数が固定なら関数フレーム内に置くstack-allocated方式、8個までのdeferなら`_defer`自体を作らずコンパイラが戻り経路に呼び出しを直接埋め込みdeferBitsという1バイトのフラグで管理するopen-coded方式に分かれるといい、各deferが登録時のスタックポインタとプログラムカウンタを保持することで関数単位の識別を可能にしていると説明する。

出典: https://zenn.dev/wakame_atsushi/articles/ab6acf1de00bed

## 15. gherkin と skills でエージェントがブラウザを操作してテストする

sonicmoovさんの記事。E2Eテストの運用工数が増え続ける課題に対し、AIエージェントが自律的にブラウザを操作してテストを実行する仕組みを構築した記事。テストケースはGiven/When/Thenで書くGherkin記法を採用し、YAMLやMarkdownではなくGherkinを選んだ理由はAIに読ませるプロンプトとしてそのまま流用しやすいためだとする。Playwright CLIを使ってAIがブラウザ操作を行うスキルを組み、全シナリオ共通の前提はBackground機能でまとめ、画面サイズはViewportsで明示、URLへの直接遷移で余計な操作を減らすといった工夫を加えた結果、12シナリオ×3回の検証で見逃しも誤検知も0件を達成し、消費トークンは1回あたり約164K、実行時間は約3分だったという。著者は検証回数がまだ少なく再現性を十分に確認できていないとして、継続的な検証が必要だと結論づけている。

出典: https://zenn.dev/sonicmoov/articles/57a9c2d0d281b5

## 16. Scribe v2 / Gemini 3.5 Transcribe / MAI-Transcribe-2 を日本語音源で実測比較してみた

minediaさんの記事。ElevenLabsのScribe v2、Google Gemini 3.5 Transcribe、MicrosoftのMAI-Transcribe-2という3つの音声認識サービスを、日本語5分間・2話者の会議音源で実測比較した記事。文字レベルの一致率は81〜86%で大意はどのサービスでも掴めるが、3社の出力が割れた28箇所を実際に聴いて判定したところMAIが17箇所で最も正確だったという。速度面ではMAIの「世界最速」という謳い文句はモード依存で、整形モードでは3.35秒とトップだが逐語+話者分離モードでは13.66秒までのび、同じモデルなのに約4倍の差が出てScribe v2の9.49秒に抜かれる場面もあった。コストはMAIが1時間あたり約0.10ドルと他社の1/2〜1/3で最安だが2026年末までの導入価格である点には注意が必要とし、整形機能の質はScribe v2が句読点とフィラー除去で最も読みやすく、議事録用途ではMAI・Scribe v2・Geminiの順で使い分けるのが実用的だとまとめている。

出典: https://zenn.dev/minedia/articles/c23bcd82102c8e

## エンディング

今日はここまでです。tsutomusaitoさんの委譲検証とhighfarさんの宣言なき制約、そしてgemcookさんのSkillSpector試用は、AIエージェントに任せる範囲が広がるほど「実際に効いているか」を裏取りする仕組みが要るという同じ課題を、ログ分析・形式検証・スキャナーというそれぞれ違う角度から示していました。1amageekさんのサンドボックス設計とmiyakonaさんの復旧設計は、境界や判断基準をあらかじめ機械的に決めておくことで、人が細かく監視しなくても安全に自由を与えられるという発想で共通していました。tomatomerdeさんの並行運用の落とし穴とmuit_techblogさんの役割分担アンケート、nobu_13さんのKiro CI/CD実験、wwwaveさんの社内QAエージェント構築は、AIと人・機械をどう組み合わせるかという設計判断を、それぞれ別の現場から具体的に見せてくれました。dgtechさんのAWS WAF誤検知とckoshienさんのVPS負荷対策は、マネージドサービスの継ぎ目や性能問題は原因を突き止めるだけでなく効いた証拠まで残して初めて解決と言えるという、地味だが避けて通れない教訓でした。sponge841841さんのLambda×SQS設計、okamyujiさんのhtmx4移行、wakame_atsushiさんのdefer実装調査、sonicmoovさんのブラウザテスト自動化、minediaさんの音声認識比較は、それぞれの技術要素を一段深く検証することの価値を教えてくれる回でした。また明日。
