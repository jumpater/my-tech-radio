# tech-radio 2026-09-22

## オープニング

tech-radio、2026年9月22日分です。今日はAIの振る舞いを実験で確かめた1本から始めます。架空の型番をでっち上げてAIに仕様表作成を頼むと本当にもっともらしく埋めてくるのか検証した記事です。続けて、AIエージェントをどこまで信じてどこで歯止めをかけるかという実践知と失敗談を7本お届けします。Claude Codeの利用制限時にCodexへ処理を引き継ぐフェイルオーバーの設計、電話予約AIの完了判定を自分で壊してしまった顛末、ホームラボの運用をClaude Codeに任せる範囲を事故から学んで4段階に切り分けた話、物理を一度も習わずに共通テスト満点を目指す家庭教師をAIエージェントとGitリポジトリで作っている記録です。さらに、LLMコスト管理の次に来るAIエージェントの実行品質管理という論点、コミットのたびに仕様書とコードの乖離を検知するOSS jev-spec、AIにコードを書かせるほどDB接続が詰まっていった話、AIにリポジトリを読ませるために秘密を持たないGitミラーを作った話と、AI活用の設計判断が続きます。中学生でもわかるLean4の最新回を挟んでから、後半はCloudflare AI Gatewayの予算ベースルーティング、Terraformのディレクトリ構成とtfstateの粒度、Amazon Quickの認証を4軸で整理した前後編、AWS Lambdaのリトライ挙動整理、Rails 8とAWS無料枠で個人開発サイトを運用する構成、AgentCore Memoryの挙動を実測した2本、Next.jsのビルドは通るのにAmplifyへ出せなかった話、MySQL8.4のオンラインDDL備忘録、個人用Linux環境にOCIを選んだ比較検討まで、地に足の着いた実務知識を10本続けます。合わせて21本、今日も中身のある記事だけを選んでお届けします。

## 1. 架空の型番の仕様表をAIに頼んだら、10回とも表は1行も出なかった

numarnさんの記事。その場ででっち上げた架空の型番についてAIに仕様表作成を頼む実験を行っている。AIは知らないことでももっともらしく埋めてくるだろうという予想のもと35回試行したところ、架空の型番では10回とも表が1行も出ず、実在を確認できないという断りが返ってきたという。同じ頼み方のまま対象を実在する規格に差し替えると10回とも49〜62行の表が出現し、「架空のサンプルで構わない」と一言添えると5回とも表は出るが毎回「架空」である旨が明記されたとしている。さらに「わからなければこの一言だけ返して」と逃げ道を用意すると10回とも1秒程度で短い返答のみになった一方、「検索してみましたが」と書かれた応答のログを調べると実際の検索回数が0だったケースも見つかったという。AIが根拠なく埋めてくるという予想は外れ、架空の対象には自動的に一線を引く挙動だったと結論づけている。

出典: https://zenn.dev/numarn/articles/fictional-device-spec-refusal-handson

## 2. Claude Codeの利用制限でも国試演習を止めない — Codexへのフェイルオーバーを作った

med_ai_studyさんの記事。医学部国家試験の演習システムをClaude Codeで回していたが、週次の利用上限に達するたびに演習が止まっていた問題を、Codexへのフェイルオーバーで解消した記録。同じプロンプトをCodexに渡すだけでは動かず、採点やフィードバック生成、履歴記録がClaude Code専用のhookに依存していたことが原因だったという。そこで演習の状態を提示・回答・公式解答照合・採点・フィードバック・記録という段階に分解して共通のPython層に管理を寄せ、各AIはその薄い入り口として扱う構成に作り替えた。提示した内容のハッシュと時刻を状態として保存することで誤った問題への記録を防ぎ、処理が途中で再実行されても記録が重複しないことも検証したとしている。AIを交換可能にする本質はプロンプトを揃えることではなく、状態や副作用、永続化をどう切り分けて設計するかにあると結論づけている。

出典: https://zenn.dev/med_ai_study/articles/kokushi-exercise-failover-claude-code-codex

## 3. 「承りました」の次に「その日は貸切でした」と言われたら。電話AIの完了判定を自分で壊した話

foriforiさんの記事。電話予約を行うAIエージェントOathraでは、完了したかどうかをモデル自身には判断させず、相手の発言だけを読む別の処理で判定する方針を採っていたが、その判定処理自体に誤りがあったことに気づいた記録。同じ依頼に対する5つの返答例を検証したところ、「仮」と「確定」という語が混在する返答、日付が1日ずれている返答、貸切で予約できないという取り消しの返答を、いずれも正しく処理できていないケースが見つかったという。空きがないことを示す語が確定を示す語より前に出た場合に限って取り消しと判定するようロジックを修正し、英語版で確定を示す語彙リストが更新されていなかった不備もあわせて直したとしている。1万件規模の敵対的な対話パターンで誤って完了と判定する件数をゼロにすることを目標に据え、言語ごとのテスト不足がそのまま脆弱性につながると述べている。

出典: https://zenn.dev/forifori/articles/oathra-five-ways

## 4. ホームラボを Claude Code に任せて6週間。どこまで任せて、どこで歯止めをきかせるか

momohikiさんの記事。自宅と、遠隔でサポートしている知人宅の機器の運用をClaude Codeに任せてきた6週間の記録。任せ方を4段階に整理していて、システムに影響を与えない確認作業は自由に任せ、機器の中で完結し元に戻せる作業は事後報告で済ませ、ルーターの再起動や電源操作、外部公開など家全体に波及する操作は都度の承認制とし、パスワードの表示やアカウント作成、金銭が絡む操作は人が行うと線引きしたという。この基準は、ルーターを無断で再起動されて通信が止まったり、指示していない設定を追加されたり、バックアップ先の容量を圧迫されかけたりした実際の事故を踏まえて決めたとしている。戻せるかどうか、影響が機器の中で閉じるかどうか、人が今使っているかどうかという3点で段階を判断し、その理由もあわせてAIの記憶に残すことで、似た状況での適切な判断を促す運用にしていると述べている。

出典: https://zenn.dev/momohiki/articles/homelab-ai-delegation-levels

## 5. 物理ゼロから共通テスト100点を目指す家庭教師を、AIエージェントとGitリポジトリで作っている

kasouzouさんの記事。物理を一度も習ったことのない状態から大学入学共通テストで満点を狙う学習を、Claude CodeとCodexを使い、進行管理をGitリポジトリで一元化する形で続けている記録。チューター役が学習順序や教材、出題、採点を決め、学習者は目の前の課題に集中するという役割分担を早い段階で固定し、進行状態を細かく区分したファイルに記録することでセッションが途切れても正確に続きから再開できるようにしたという。会話ログは手作業での書き起こしをやめて自動で取り込む方式に切り替え、表示内容と保存内容がずれるリスクを取り除いた。入力補完機能が正解を先に表示してしまう問題は設定で無効化し、教材を公開するスクリプトが不安定だった際はエラーログを全文確認する姿勢を徹底したとしている。目指したのは賢い先生を作ることではなく、担当が入れ替わっても同じ指導が続く仕組みを作ることだったとまとめている。

出典: https://zenn.dev/kasouzou/articles/ai-tutor-physics-repository

## 6. LLMコスト管理の次に来るもの：AIエージェントの実行品質を管理する

infra_ojisanさんの記事。LLM利用のコスト管理の次に必要になる課題として、AIエージェントの実行そのものの品質を管理する仕組みを論じている。組織のLLM運用はまず誰がどのモデルをいくら使ったかを管理する段階から始まるが、次に必要なのはどの指示やツールが使われ、同じ業務なのになぜ結果が変わるのかを見えるようにする段階だと位置づける。エージェントの自己申告だけでは実際に起きたことを証明できないとして、ファイルシステムやAPI呼び出しといった外部の記録と突き合わせる必要性を指摘し、観測した結果に直接取得・相互確認・外部確認・推定・確認不可といった証拠の強さを付けて扱う設計を提案している。目指すのはAIの推論そのものを決定論的にすることではなく、AIを扱う周囲の運用処理を決定論的にすることであり、完全な説明を求めるより証拠の限界を明示することで現場が信頼できる範囲を保てる体制を作るべきだとしている。

出典: https://zenn.dev/infra_ojisan/articles/llmops-next-agent-runtime-governance

## 7. AI が速くコードを書く時代に、仕様書との乖離を止めるために jev-spec という OSS を作っている話

nozomi_coboさんの記事。AIがコードを高速に生成する現場で、仕様書と実装の乖離が誰にも気づかれないまま積み重なる問題に対応するため、コミットのたびに乖離を検知できるOSS jev-specを開発した記録。従来のLLMによる検証は実行に数分かかりトークン課金も発生するため、コミットのたびに走らせるフックとしては使えなかったという。判断に特化したAIモデルJevは、与えられた文脈を一度の評価で処理し確率やスコアを直接返す性質を持つため応答が数百ミリ秒で終わることに着目し、Markdownの仕様書から検証項目を抜き出して実装コードと照合する4段階の仕組みを作った。著者自身のリポジトリで実行したところ、8つの対象・22項目の判定が1回あたり約4秒、0.0006ドル程度で完了し、同じコードに対する繰り返し判定でも確率のばらつきはごくわずかだったとしている。AI時代の開発のリズムを崩さない速度で仕様と実装の一貫性を保つ、実践的な解決策として提示している。

出典: https://zenn.dev/nozomi_cobo/articles/jev-spec-introduction

## 8. AI に書かせるほど DB が詰まる — コネクションプールは「1本ずつ正しい」では足りない

maruchi777さんの記事。AIに機能追加を任せる速度が上がった結果、最初に壊れたのはデータベースだったという記録。個々のコードを見れば正しく、テストも通るのに、データベースの接続上限エラーが起きる現象を扱っている。原因は、並列処理で同時に何本の接続が走るかがコードに明示されていないこと、各アプリが個別に設定する接続プール数の合計を誰も把握していないこと、サーバーレス環境では実行のたびに新しい接続要求が積み上がってしまうことの3つだという。対策として、並列度を明示的に宣言する書き方に改め、システム全体の接続数を1つの表にまとめて可視化する運用に切り替えたとしている。接続は有限の資源であり誰かが数えなければならず、AIには周辺の文脈が見えていない以上、システムレベルの制約は人間側から伝える必要があると結論づけている。

出典: https://zenn.dev/maruchi777/articles/f07e5483fb24e4

## 9. 秘密を持たないGitミラー「pubmir」を作った

desktopgameさんの記事。Claude CodeやChatGPTにリポジトリを読ませて相談する機会が増える一方で、IPアドレスやホスト名、APIキーなど外部に出したくない情報が混ざっている問題に対応するために作ったCLIツールpubmirの紹介。実値を持つ非公開リポジトリとは別に、秘密情報をプレースホルダーに置き換えた匿名化済みのミラーリポジトリを維持する仕組みで、同じ値は常に同じプレースホルダーに変換されるため、AI側から見ても過去から現在まで同一の対象を指しているとわかるようにしているという。置換のルールを書く設定ファイルと秘密の実値を書くファイルを分離し、存在自体を隠す指定とプレースホルダーに置き換える指定を使い分けられる。権限設定やフックで制御するのではなく、AIが触れるリポジトリそのものを最初から匿名化しておくという選択をしたのは、Git履歴を含めた文脈は消したくなかったからだとしている。秘密は持たないが意味は保っているリポジトリを作ることが狙いで、AIへの相談だけでなくバックアップや将来の公開用途にも同じミラーを使い回せる設計になっている。

出典: https://zenn.dev/desktopgame/articles/8b059e44f32750

## 10. 中学生でもわかる Lean 4 #07｜「定義した」と「証明した」は違う

dantargさんの記事。「中学生でもわかるLean4」シリーズ第7回として、定理証明支援系Lean4で「定義した」ことと「証明した」ことは別物だという点を扱っている。見るからに強そうな名前の命題であっても、その中身は定義がどう書かれているかによって決まり、名前そのものが新しい事実を証明するわけではないと説明する。定義を中身のない当たり前の主張にしてしまえば、いかにも重要そうな名前の定理の証明もほとんど自明になってしまう例を示し、強そうな名前の定理を見つけたときは証明だけでなく定義まで遡って読む必要があると説く。形式的な中身と、その見た目の表現は切り離して考えるべきだという教訓を今回の締めくくりとしている。

出典: https://zenn.dev/dantarg/articles/lean4-definition-vs-proof

## 11. Cloudflare AI Gateway 予算ベースAIモデルルーティング

takaakisuzukiさんの記事。複数のLLMを使い分ける際、予算に応じてモデルを自動的に切り替えたいという悩みに対し、Cloudflare AI Gatewayの支出上限機能と動的ルーティングを組み合わせる方法を解説している。高性能モデルを優先しつつ予算を使い切ったら段階的に安価なモデルへフォールバックする3段構成の例を示し、ユーザーやチームごとに予算を分けたい場合はメタデータのパーティション機能を使うとルール数を増やさずに管理できるとしている。キャッシュがヒットした場合はコストが発生しないことや、Workers AIは予算追跡の対象外になるため最終的なフォールバック先に向いていることなど、実運用上の細かな注意点もあわせて紹介している。利用者側には常に成功レスポンスを返しながら、内部では段階的にコスト効率の良いモデルへ切り替えていく設計が実現できるとまとめている。

出典: https://zenn.dev/takaakisuzuki/articles/e6939a04f9ea9b

## 12. Terraformのディレクトリ構成とtfstateの粒度について

h_tさんの記事。Terraformの構成設計を、ディレクトリの分け方とtfstateの粒度という2つの独立した軸で整理している。ディレクトリはリソース種別ごとにまとめる構成とアプリケーション単位でまとめる構成があり、前者は共通部品を使い回しやすい一方で複数人が同時に触るとコンフリクトが起きやすく、後者はアプリ間の依存が少なくコンフリクトが減る一方で設定の重複が生じやすいという。tfstateについても、1つにまとめればoutputsの受け渡しが不要になる代わりにplan・applyが肥大化して失敗時の調整が難しくなり、細かく分ければ失敗の影響を局所化できる代わりにoutputsの受け渡しと依存順序の管理が複雑になるとしている。2軸の組み合わせを整理した上で、アプリケーション単位の構成とtfstateの細分化は相性が良いと結論づけ、著者自身も今後は1つのtfstateにまとめる構成を避けるかもしれないと述べている。

出典: https://zenn.dev/h_t/articles/terraform-directory-structure-20260922

## 13. Amazon Quick の認証を4軸で整理して、Entra ID とデスクトップ SSO を繋いでみた【前編：4軸の整理】

aws_japanさんの記事。Amazon Quickのエンタープライズサインインを Microsoft Entra ID と連携させる作業は素直に進めると一度は詰まると前置きした上で、認証の混乱をアカウント種別、ID方式、Quick Web側のシングルサインオン設定、エディション・サブスクリプション・ロールという4つの独立した軸に分解して整理した前編。SSO継続のボタンを押してもEntra IDではなくQuickのパスワード入力画面が出てしまう挙動や、ユーザーが見つからないというエラー、発行者URLの記載漏れによって拒否される挙動など、実際につまずいた事例をそれぞれの軸に紐づけて説明している。特に紛らわしいのは「エンタープライズ」という語がアカウントの種別、エディション、ユーザーのサブスクリプションという3つの異なる概念を指してしまうことだとし、デスクトップアプリのSSOには外部のOIDC IdPが必須で、IAM Identity Center単体では構成できない点もあわせて明らかにしている。

出典: https://zenn.dev/aws_japan/articles/amazon-quick-auth-4-axes-part1

## 14. Amazon Quick の認証を4軸で整理して、Entra ID とデスクトップ SSO を繋いでみた【後編：実機検証】

aws_japanさんの記事。前編で整理した4軸に沿って、Microsoft Entra IDを使ったデスクトップSSOを実機で検証した後編。Quick Webのサインインとデスクトップアプリのエンタープライズサインインは完全に別のレイヤーで、前者はSAML、後者はOIDCという異なるプロトコルを使う点が最大のポイントだとしている。パスワード認証のまま一部だけEntra連携するパターン、WebとDesktop双方をEntra IDに寄せるパターン、IAM Identity Center経由でグループ単位のロール割り当てまで対応するパターンの3通りを試し、Entra ID側のメールアドレスが既定では未設定になっていて完全一致が求められる点や、フェデレーション側とネイティブ側で同じメールアドレスのユーザーが重複して衝突する点など、公式ドキュメントの説明の裏にある制約を実機で裏付けている。新規に構築するならIAM Identity Center経由の構成を勧める一方、既存環境からの段階的な移行には別のパターンが向くとまとめている。

出典: https://zenn.dev/aws_japan/articles/amazon-quick-entra-id-desktop-sso-part2

## 15. AWS Lambdaのリトライと失敗時の挙動を整理する

h_tさんの記事。Lambdaのリトライ設定が画面上は存在するのに実際には効かないという混乱を解消するため、呼び出し方式ごとの挙動を整理している。API GatewayやALB経由の同期呼び出しではLambda自体はリトライせず、エラーはそのまま呼び出し元に返る。S3やEventBridgeなどの非同期呼び出しではLambda内部のキューがリトライを担い、成功時・失敗時の送信先をDestinations機能で指定できる。一方でSQSやKinesisなどのポーリング型では、Lambdaがイベントソース側をポーリングして関数を呼び出す形になっており、リトライの管理はイベントソース側に委ねられ、Lambda側の管理画面上のリトライやDLQ設定は実質機能しないという。SQSトリガーで再送を制御したい場合はSQS側の受信回数の上限を設定するのが正しい方法であり、想定外のエラーに備えてメトリクス監視を組み合わせることを勧めている。

出典: https://zenn.dev/h_t/articles/lambda-retry-failure-20260921

## 16. Rails 8とAWS無料枠でアニメ番組表を運用している構成

anitickさんの記事。個人開発のアニメ番組表サイトを、RedisもALBも使わずRails 8とAWSの無料枠に収まる構成で運用している記録。Solid Queue・Solid Cache・Solid CableをすべてPostgreSQL上で動かすことでRedis相当のマネージドサービスを不要にし、ALBとNAT Gatewayも廃止して月々のコストを大きく抑えたという。外部データソース4本を毎時取り込む処理では、各APIが自主的に決めている呼び出し上限から1回あたりの取得件数を逆算し、成功した取り込みだけに完了時刻を記録して障害時は次の周期に持ち越す設計にし、あるAPIの障害が他のAPIの取り込みに波及しないようサーキットブレーカーを個別に持たせているとしている。ページネーションを50行弱の自作コードで済ませるなど依存を絞り込む判断も紹介しており、小さなサーバー構成1台ずつでも止まらないサイトが作れることを実例で示している。

出典: https://zenn.dev/anitick/articles/b3dee7708c5d77

## 17. AgentCore Memoryのdata eventを実測 — 削除台帳はCloudTrailだけで作れる

kashiwabarayさんの記事。Amazon Bedrock AgentCore Memoryの操作がCloudTrailのdata eventとしてどこまで記録されるかを実測した記録。有効化するとレコードの削除や取得、イベント作成など複数の操作がdata eventとして残ることを確認し、レコードを直接指定する削除・取得操作はレコードIDまで記録される一方、意味検索による取得操作はnamespaceまでしか記録されず、実際にどのレコードが返されたかは特定できないという粒度の差を明らかにしている。この性質から、いつどのIAM主体がどのレコードを削除したかという台帳はCloudTrailだけで作成できると結論づける一方、検索経由でのアクセス頻度を数えるような用途には別途アプリケーション側での記録が必要になるとしている。

出典: https://zenn.dev/kashiwabaray/articles/83ca650003fa24

## 18. AgentCore Memoryのライフサイクル管理を実測 — namespaceは素の文字列prefix一致で1文字でも当たる

kashiwabarayさんの記事。同じ著者によるAgentCore Memoryの実測記録で、長期記憶レコードの自動削除の実態を検証している。最大の発見は、namespaceの指定が素の文字列の前方一致で判定され、1文字だけ指定してもそこから始まる全レコードがまとめてヒットしてしまう挙動で、ワイルドカード指定は使えず0件が返ってくるだけだったという。さらに短期イベントの保持期限と長期記憶レコードの保持は別物で、短期イベントが期限切れで消えても、そこから抽出された長期レコードは自動では消えないことを実際に確認している。時刻を使った経過判定に使えるメタデータは作成日時と更新日時の2つに事実上限られ、独自の日時型のタグも持たせられないなど、公式のTTL方式は作成日時基準の単純な判定にとどまり、複雑な保持戦略を組むには自前の削除ジョブ実装が欠かせないと結論づけている。

出典: https://zenn.dev/kashiwabaray/articles/710c6dc9b899eb

## 19. Next.jsのビルドは通るのにAmplifyへ出せない。成果物を254.2→168.3 MiBに減らした話

yamato_snowさんの記事。Next.jsアプリのビルド自体は成功しているのに、AWS Amplifyへのデプロイだけが成果物サイズの上限超過で失敗するという事象を調査した記録。原因は、設定で除外したはずのライブラリのWebAssemblyファイルがNext.jsの依存ファイル一覧からは消えていても実際の配信成果物には残っていたことと、画像処理ライブラリの複数バージョンが環境ごとに重複して同梱されていたことの2つだったという。ビルド完了の表示と、その後に行われる成果物サイズの検査は別の工程であることに気づき、使わない環境向けのファイルを配信直前に削除するスクリプトと、想定外の実行環境では処理を止める検証スクリプトを追加することで、配信成果物を254.2MiBから168.3MiBまで削減してデプロイに成功させたとしている。設定上除外したことと配信ファイルから実際に消えていることは別の確認が必要で、失敗時は実際に生成された成果物そのものを開いて確かめるべきだと結んでいる。

出典: https://zenn.dev/yamato_snow/articles/amplify-nextjs-build-output-size

## 20. MySQL 8.4のオンラインDDL備忘録

sloppybookさんの記事。本番テーブルへのALTER TABLE実行のたびに毎回同じことを調べ直している状況を解消するため、MySQL8.4のInnoDBを対象にオンラインDDLの挙動を1か所にまとめている。実行方式はメタデータのみ書き換えて数ミリ秒で終わるINSTANT、InnoDB内部で作り直しつつ読み書きを続けられるINPLACE、一時テーブルへコピーし書き込みを止めるCOPYの3種類に整理され、指定がなければMySQLがこの順で実行可能な方式を自動的に選ぶという。カラムの追加・削除やデフォルト値変更、テーブル名変更などはINSTANTで完結しメンテナンス時間が不要になる一方、テーブルサイズの事前確認やメタデータロックによる影響、行バージョンの上限、オンラインDDLログの容量制限、レプリカへの遅延など見落としやすい注意点も列挙している。メンテナンス時間を確保せずに済むかどうかが実務判断の最大の分かれ目であり、アルゴリズムを明示的に指定して事故を防ぐことを勧めている。

出典: https://zenn.dev/sloppybook/articles/aa6691f19793f0

## 21. 【徹底比較】Linux環境にOCIを選んだ話｜OCI・AWS・Azure・Google CloudとRaspberry Pi・ミニPC

rioseccalocaさんの記事。普段使っているWindows機とは別にLinux環境が必要になり、クラウドのIaaSと物理端末(Raspberry Piやミニ PC)を比較した上で、最終的にOracle Cloud Infrastructure(OCI)を選んだ記録。求めていたのはSSH接続でCodexやPythonが使え、24時間動かし続けられて初期投資を抑えられる環境で、IaaSは初期費用や設置スペースが不要な一方、物理端末はUSBやGPIOなどハードウェアを直接操作できるという違いがあるとしている。OCIを選んだ決め手は、物理端末を買う前にある程度しっかりしたLinux環境を無料で試せる点で、Arm系の無料枠なら2 OCPU・12GBメモリまで無料で使えるという。現在はAMD系とArm系のインスタンスを用途によって使い分けながら、個人の開発環境として安定して運用していると述べている。

出典: https://zenn.dev/rioseccaloca/articles/d14f23a79060e8

## エンディング

今日はここまでです。numarnさんの仕様表拒否実験は、AIがもっともらしく埋めてくるという先入観を数値で裏切ってくれました。med_ai_studyさんのフェイルオーバー設計、foriforiさんの完了判定の修正、momohikiさんの委任レベル整理、kasouzouさんの家庭教師システムは、AIエージェントにどこまで任せてどこで人が線を引くかという同じ問いに、それぞれ違う現場から答えを出していました。infra_ojisanさんの実行ガバナンス論とnozomi_coboさんのjev-specは、AIの出力を信じきらずに検証し続ける仕組みづくりという点でつながっていましたし、maruchi777さんのDB接続詰まりとdesktopgameさんの秘密を持たないGitミラーは、AIに任せる範囲が広がるほど人間側の設計がむしろ問われることを示していました。dantargさんのLean4シリーズを挟んでからは、takaakisuzukiさんのAI Gatewayルーティング、h_tさんのTerraform構成論とLambdaリトライ整理、aws_japanさんのAmazon Quick認証4軸、anitickさんの無料枠運用、kashiwabarayさんのAgentCore Memory実測2本、yamato_snowさんのAmplifyビルド調査、sloppybookさんのMySQL DDL備忘録、rioseccalocaさんのOCI比較と、地に足の着いたインフラ・データベースの実践知が続きました。AIをどう扱うかという問いと、日々の基盤を一つずつ固める作業、その両方が同じ日に並ぶのがtech-radioらしいところだと思います。また明日。
